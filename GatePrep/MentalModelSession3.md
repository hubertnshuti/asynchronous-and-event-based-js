1. What is the difference between `Promise.all`, `Promise.any`, `Promise.race`, and `Promise.allSettled` — and when would you use each?
2. Why are Promises considered *microtasks* and how are microtasks prioritized in the event loop compared to macrotasks?
3. How do you handle errors within an `async` function when using `await`?
4. If JavaScript is single-threaded, how can it still handle multiple asynchronous operations at once?
5. What is Fetch API?
    1. How does it differ from older `XMLHttpRequest`?
    2. What is a **`Request`** object and `Response` object in the Fetch API, and what can it represent?
    3. What is the purpose of the `headers` property in `Request` and `Response` objects?
    4. Is it possible to cancel a fetch request if it takes too long or if the response is no longer needed?
6. What is the use of the `.finally()` method in Promise chaining?
    1. What types of outcomes (fulfilled or rejected) trigger `.finally()`?
    2. Why is `.finally()` useful for resource cleanup or final operations?
7. What is the **`memory heap`** in JavaScript, and how does it differ from the **`call stack`**?
    1. What types of data are stored in the heap vs. the stack?
    2. Why are objects, arrays, and functions allocated in the heap instead of the stack?
8. How does AJAX enable updating parts of a webpage without reloading the entire page?
9. What is execution context and what is the difference between function execution context and global execution context?
10. What is the difference between `Debouncing` and `Throttling`?